{
  "name": "seal-verify-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action: verify Seal decision receipts by re-deriving each verdict from the receipt's own policy and call",
  "license": "Apache-2.0",
  "author": "Ben Cassie",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "gen:fixtures": "node scripts/gen-fixtures.cjs",
    "test": "node --test"
  }
}
